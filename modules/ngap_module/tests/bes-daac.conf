#
#  Switch to only using the DAAC S3 buckets for the DMR++ documents.
#
BES.Include = bes.conf

# Use this with tests to force the use of the DAAC bucket even though the granules
# are in our (cloudydap) bucket too. jhrg 8/9/24
NGAP.UseOPeNDAPBucket = false
