#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	$(MAKE) DESTDIR=$$(pwd)/debian/bes prefix=/usr install

override_dh_auto_configure:
	dh_auto_configure -- --with-dependencies=/home/ubuntu/src/hyrax/build/deps
